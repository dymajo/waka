import 'mocha'
import { expect } from 'chai'

describe('waka-orchestrator/adaptors/keyvalueDynamo', () => {
  it('should return hello world', () => {
    const result = 'Hello world!'
    expect(result).to.equal('Hello world!')
  })
})
